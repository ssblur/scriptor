name: fabric

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    environment: publish
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - id: changelog
        uses: ssblur/git-changelog-action@v1
      - uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: '17'
      - name: Build Forge
        run: bash ./gradlew fabric:build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USER: 'ssblur'
      - name: Cleanup
        run: |
          rm */build/libs/scriptor-*-dev-shadow.jar
          bash -c 'for file in fabric/build/libs/*.jar; do mv "$file" "${file%.jar}-fabric.jar"; done'
          bash -c 'for file in forge/build/libs/*.jar; do mv "$file" "${file%.jar}-forge.jar"; done'
      - name: Extract Tag Version
        id: tag
        run: echo "version=${GITHUB_REF:11}" >> $GITHUB_OUTPUT
      - name: Deploy
        uses: ssblur/minecraft-publish@970e65141a6c69d7b6f08f3ff0aa84d3fc356e6d
        with:
          mod-file: forge/build/libs/scriptor-${{ steps.tag.outputs.version }}-forge.jar
          version-name: "${{ steps.tag.outputs.version }}-forge"
          minecraft-version: 1.20.4
          mod-loader: forge
          changelog: ${{ steps.changelog.outputs.changelog }}

          curse-project-id: "817436"
          curse-token: "${{ secrets.CURSEFORGE_TOKEN }}"
          curse-relations: "architectury-api:requiredDependency"

          modrinth-project-id: "scriptor-magicae"
          modrinth-token: "${{ secrets.MODRINTH_TOKEN }}"
          modrinth-dependencies: '[{"project_id": "architectury-api", "dependency_type": "required"},{"project_id": "fabric-api", "dependency_type": "required"}]'

          jar-signing-store: ${{ secrets.JAR_SIGNING_STORE }}
          jar-signing-alias: ${{ secrets.JAR_SIGNING_ALIAS }}
          jar-signing-store-pass: ${{ secrets.JAR_SIGNING_STORE_PASS }}
          jar-signing-key-pass: ${{ secrets.JAR_SIGNING_KEY_PASS }}